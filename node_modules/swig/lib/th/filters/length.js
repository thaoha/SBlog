/**
 * Created by HaThao on 7/30/14.
 */

var isArray = require('../utils').isArray;

module.exports = function(input) {
    if ('string' === typeof input || isArray(input)) {
        return input.length;
    }
    return 0;
}